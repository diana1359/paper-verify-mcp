services:
  - type: web
    name: paper-verify-mcp
    runtime: docker
    plan: free
    healthCheckPath: /mcp
    envVars:
      - key: CONTACT_EMAIL
        sync: false      # 배포 시 대시보드에서 입력
      - key: NCBI_API_KEY
        sync: false      # 선택
